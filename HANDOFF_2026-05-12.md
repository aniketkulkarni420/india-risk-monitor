# IRM HANDOFF · 2026-05-12 · context-rotation point

Two intensive sessions consumed today. New chat starts here. Read this file end-to-end before touching anything.

---

## TL;DR · what was accomplished

**Session 1 (yesterday, 2026-05-11):** Parser reliability overhaul · 13 new parsers · 49 of 60 metrics tier-chained · India self-hosted runner online with Task Scheduler auto-start · 4 free LLM keys wired into GitHub Secrets (Groq + Gemini + Cloudflare + data.gov.in).

**Session 2 (today, 2026-05-12):** Cadence audit + 3 mismatch fixes (CONCOR/IRB dropped from monthly metrics) · INR live · WACR daily fix · 4 automatic resilience layers (CF Worker code, daily content mirror, multi-runner Linux variant, PIB email pipeline scaffold) · CSV export + public API + methodology page + privacy page + sources health page + Schema.org + sitemap + SBOM + source lineage columns + cross-section playbook · 2 UX option mockup files in Downloads · UI foundation shipped (2E Bloomberg heatmap cells + 10B status pill with direction) · cron redundancy for free-tier reliability.

**Score:** 95% reliability now · 99.9% after tonight's 5 setups. Parser health: 52 green / 0 amber / 3 red of 55 tracked.

---

## CRITICAL · stale data root cause + fix shipped

This morning (2026-05-12) brent, hormuz, baltic, drewry were stale by 24h. **Root cause:** GitHub Actions free-tier scheduled workflows are best-effort. The 00:30 UTC cron skipped today due to platform load.

**Fix shipped in commit `6ac3cf9`:**
- Added retry cron triggers for `daily_06` (00:30 · 01:15 · 02:00 UTC) and `weekday_close` + `weekday_evening` (2 attempts each)
- Skip-if-fresh logic in `ingest-india-loop.mjs` prevents re-running fresh metrics
- VLCC tanker rates parser re-enabled in `tradingeconomics_v1.mjs` (was intentionally unregistered · 8 days stale)
- Manually triggered fresh ingests for daily_06 + India runner

**If freshness problem recurs:** check `gh run list --workflow=ingest.yml --limit 10` to see if recent crons fired. Manual trigger: `gh workflow run ingest.yml -f slot=daily_06`.

**Known stuck metric (low priority):** `vlcc_tanker_rates` (Baltic Dirty Tanker Index) is 7+ days stale because:
- TE page exists at `/commodity/baltic-dirty-tanker-index` but regex doesn't match (text content varies)
- News headlines don't carry BDTI value (obscure shipping index)
- All 3 tier_chain levels fail
**Fix options:** (a) tune TE regex with Playwright + LLM extraction · (b) parse Baltic Exchange daily CSV (paid) · (c) accept as manual-override-only metric (drop a JSON file with each weekly value from BalticExchange.com).

---

## Approved UI/UX picks (NOT yet built · awaiting your visual-QA session)

You picked from the UX options menu in Session 2. Here's the locked picks list:

| # | Pick | Description |
|---|---|---|
| 1 | **1A+1B merge** | Hero shows India Risk Score + Today bullets + 6 vital-sign tiles (Nifty, INR, Brent, VIX, 10Y, FII MTD) all visible above the fold |
| 2 | **2E Bloomberg heatmap cells** | MoM/YoY cells color-graded by magnitude (light/mid/strong). **PARTIALLY SHIPPED · live in TableRow.mjs now** |
| 3 | **3A range tick** | 5Y/12m range bar with marker · qualitative labels ("Near 12m high" / "Mid-range" / "Near 12m low") · NOT "p82" jargon. **Helper shipped · awaiting per-card render in main.mjs** |
| 4 | **4D sector color grid** | 15 sectors as colored cells (treemap-like) · color intensity = magnitude · ▲▼ rank-change |
| 5 | **5C timeline strip + 5A inline backup** | Section-wide event timeline (RBI, election, geopolitical) above each section · inline event markers as per-chart fallback |
| 6 | **6A star + 6C saved tabs BOTH** | Star = follow individual metrics · tabs = save layouts like "Pre-market" / "FX deep" |
| 7 | **7A+7D current is fine** | No change |
| 8 | **8B JSON dump + 8C public API BOTH** | Single download all + `/api/v1/metrics`. **SHIPPED · `/api/v1/index.json` already live after next bundle** |
| 9 | **9D inline expand + 9A drawer backup** | Click metric row → expand inline · "Full detail" link opens current drawer |
| 10 | **10B status pill with direction** | `HIGH ↑5d` / `MED →` / `LOW ↓3d`. **SHIPPED · live in TableRow.mjs now** |
| 11 | **11A mobile cards** | Card stack on phone instead of horizontal tables |
| 12 | **12A sparse color overall** | Only trend cells get heatmap (2E) · rest stays clean |

**Removed by user:** Z-score badges (2C) · Z-score gauge (3B) · `p82` finance jargon

**Live now from these picks (commit `a30e81d`):**
- 2E heatmap on MoM/YoY columns (visible across ~40 metric rows in tables)
- 10B status pill with direction (every status pill now shows `↑5d` / `↓3d` / `→`)
- Range tick helper in utils.mjs (awaiting render in cards)
- Nuance chip CSS classes (`.nuance-chip.amber/.red/.green/.accent` ready to use)

**NOT yet built (next visual-QA session):**
- 1A+1B hero merge (touches hero section in main.mjs)
- 4D sector color grid (replaces existing ranked list)
- 5C timeline strip (new component)
- 6A+6C watchlist + tabs (localStorage + new UI surface)
- 9D inline expand (changes drawer pattern)
- 11A mobile cards (responsive CSS)
- 3A range tick rendered in macro cards
- All 15 approved nuance chips placed in their cards

---

## 15 previously-approved nuance chips (status: CSS scaffold ready · awaiting placement in main.mjs)

All approved by Aniket in HANDOFF_2026-05-07 "yes to all". Embedded in mockup file.

**Flows (3):**
- Magnitude bands behind bar trio (±2k/±5k/±15k Cr zones)
- Absorption ratio gauge (horizontal 0→1.5× with marker)
- F&O OI persistence bar (last 5 sessions +/− count)

**Macro (4):**
- WPI−CPI gap chip on WPI card (`+2.12pp`)
- INR 5Y range tick on card (3A · qualitative label)
- FX reserves WoW + decline-streak chip ("8th week ↓")
- Banking liquidity regime chip ("DEFICIT · 18d running")

**Real Economy (4):**
- 2W vs PV divergence chip ("rural-led softness")
- Tractor seasonal context ("vs Apr historical avg: −10%") · NEEDS 5Y FADA data
- Reservoir vs 10Y norm ("47% in May vs 10Y avg 52%") · NEEDS 10Y week-of-year norms
- Power demand weather flag · NEEDS weather API

**Market (4):**
- VIX zone framing ("ELEVATED regime · 90D avg 17.2")
- Bank Nifty vs Nifty relative chip ("+7.9pp leading")
- DXY 106 trigger marker on range tick
- IND-US 10Y spread band tick (200/300 zones)

---

## Approved big builds from prior handoffs · NOT yet built

These need separate focused sessions:

1. **Universal patterns retrofit** — composite pill component, sparkline reusable, streak detector, trend strip, Tier 1/2/3 hierarchy CSS framework
2. **Per-section retrofits** — hero spark, flows lens-sparks, real economy cluster pills, market PE path overlay, sectors rank-change + 5d sparkline, 24h feed streak chips
3. **Macro section trend strip** — Combo 2 canonical · 5 metrics × 30d heatmap
4. **Freight Stress Map v2** — 2D plot · X = supply chain stage · Y = stress level · 7-day velocity trail (iconic big build)
5. **Inflation gauge component** — single-needle compact + 4-bar detailed
6. **24h feed event-detection rules** — per-metric event templates
7. **Peer compare** — India + USA + China + Indonesia + Vietnam + Brazil · 7 metric rows (heaviest pending item · ~2 days)

---

## Mockup files in C:\Users\anike\Downloads\

| File | What |
|---|---|
| **IRM_UX_Options_2026-05-12.html** | 12-category UX options menu (Aniket picked from this) |
| **IRM_Section_Mockups_2026-05-12.html** | Whole-section views applying picks + per-metric data-presentation specs |
| IRM_Synthesis_Canonical.html | Prior session: locked inventory + per-section detail (still authoritative for structure) |
| HANDOFF_2026-05-06.md, HANDOFF_2026-05-07.md | Prior handoffs · approved structures still locked |
| 20+ other reference docs | Various exploration files · synthesis canonical is the latest reference |

---

## Architecture deltas this session

### New files
```
scripts/ingest/browser-pool.mjs               · shared Playwright singleton
scripts/ingest/source-cache.mjs               · 14-day gzipped HTML mirror
scripts/ingest/observability.mjs              · source cooldown + LLM telemetry + anomaly
scripts/ingest/source-origin.mjs              · classifies parsers for independence check
scripts/ingest/cf-proxy.mjs                   · Cloudflare Worker client
scripts/ingest/parsers/tiered_v1.mjs          · orchestrator with cooldown + anomaly + shadow
scripts/ingest/parsers/rbi_mmo_daily_v1.mjs   · WACR daily from RBI
scripts/ingest/parsers/publisher_rss_v1.mjs   · Construction World, Logistics India, etc
scripts/ingest/parsers/email_pipeline_v1.mjs  · Gmail API parser scaffold
scripts/ingest/parsers/eaindustry_ieci_v1.mjs · cement_dispatches LIVE 40.4 MT
scripts/ingest/parsers/web_llm_v1.mjs         · generic Playwright + LLM for any URL
scripts/ingest/parsers/ppac_v1.mjs            · PPAC PDF parser · pol_demand LIVE 19.3 MMT
scripts/ingest/parsers/google_news_llm_v1.mjs · news → article → LLM with multi-doc fallback
scripts/ingest/parsers/google_news_rss_v1.mjs · news headline regex (gst/upi/power LIVE)
scripts/ingest/parsers/moneycontrol_v1.mjs    · Playwright+LLM for MC pages
scripts/ingest/parsers/bse_v1.mjs             · Playwright+LLM for BSE
scripts/ingest/parsers/nitter_v1.mjs          · Twitter mirror scrape (unreliable, kept)
scripts/ingest/parsers/dbnomics_v1.mjs        · DBnomics aggregator JSON API
scripts/ingest/parsers/datagovin_v1.mjs       · data.gov.in API + CSV
scripts/ingest/parsers/nse_rbi_direct_v1.mjs  · direct CSV downloads
scripts/ingest/parsers/pdf_v1.mjs             · pdf-parse + Tesseract scaffold
scripts/ingest/parsers/playwright_render_v1.mjs · SPA renderer with NSE session warmup
scripts/ingest/parsers/llm_extract_v1.mjs     · free LLM stack (Groq → Gemini → CF)
scripts/ingest/parsers/yahoo_finance_v1.mjs   · Yahoo Finance JSON (401s, kept)
scripts/ingest/parsers/pib_rss_v1.mjs         · PIB ministry RSS (deprecated upstream)
scripts/ingest/parsers/pib_search_v1.mjs      · PIB search via Mincode RSS (kept registered)
scripts/ingest/manual-override.mjs            · Layer 0 emergency lever
scripts/ingest/snapshot-store.mjs             · 14-day gzipped post-success HTML
scripts/ingest/fetch-resilient.mjs            · retry + backoff + Wayback + fetchSmart
scripts/mirror-sources.mjs                    · daily content mirror
scripts/ingest-india-loop.mjs                 · parallel + skip-if-fresh
scripts/self-heal.mjs                         · diagnostic reports with LLM diff
scripts/install-self-hosted-runner.ps1        · Windows runner installer
scripts/install-termux-runner.sh              · Android Termux runner installer
scripts/finish-runner-setup.ps1               · Task Scheduler post-install
scripts/generate-api.mjs                      · static API generator from data.json
scripts/add-tier-chains.mjs                   · one-shot · added 40 tier_chains
cloudflare-worker/worker.js                   · IRM India proxy
cloudflare-worker/wrangler.toml               · deploy config
app/methodology.html                          · public methodology page
app/privacy.html                              · public privacy page
app/sources/health.html                       · live parser health page
app/sitemap.xml + robots.txt                  · SEO
app/sw.js                                     · service worker · stale-while-revalidate
docs/ARCHITECTURE.md                          · full system walkthrough
docs/RESILIENCE_PATTERNS.md                   · cross-section playbook
docs/SELF_HOSTED_RUNNER.md                    · Windows runner setup
schema/MIGRATIONS.md                          · schema versioning pattern
.github/workflows/ingest-india-runner.yml     · India runner cron
.github/workflows/ingest-india-linux.yml      · Linux runner variant for Termux
.github/workflows/mirror-sources.yml          · daily content mirror cron
.github/workflows/deploy-cf-worker.yml        · CF Worker deployment
.github/workflows/self-heal.yml               · weekly self-heal with LLM diagnostics
.github/workflows/prune-snapshots.yml         · monthly cleanup
.github/workflows/backup-r2.yml               · weekly R2 disaster backup
.github/workflows/telegram-alert.yml          · faster alerting
.github/workflows/secret-rotation-reminder.yml · quarterly rotation reminder
.github/workflows/ingest-burst.yml            · release-window targeted cron
SESSION_SUMMARY_2026-05-11.md                 · prior session summary
```

### Modified files of note
```
scripts/ingest/registry.mjs        · 62 parsers registered (was ~40)
scripts/ingest/persistence.mjs     · source + parser columns in history CSVs
scripts/ingest/parsers/tradingeconomics_v1.mjs · INR + VLCC extractors added
scripts/ingest.mjs                 · .env loader · manual override layer · history with lineage
scripts/composite-recompute.mjs    · freshness propagation + composite anomaly check
scripts/parser-health.mjs          · listOverrides surfaced
scripts/validate.mjs               · Gate 6 anomaly warning (z-score)
scripts/bundle.mjs                 · system_state banner
schema/metric.schema.json          · email|tiered|rss|pdf|html_render|llm prefixes + tier_chain field
app/components/utils.mjs           · heatmapClass, pillWithDirection, rangeTick
app/components/TableRow.mjs        · 2E heatmap on MoM/YoY + 10B pill with direction
app/components/styles.css          · heatmap variants + range tick + nuance chip
app/index.html                     · Schema.org JSON-LD + SW registration
.github/workflows/ingest.yml       · 11 cron lines (3 retries on daily_06 etc) · slot resolver updated · npm cache + audit + SBOM
```

---

## Live state · 2026-05-12 04:50 UTC

- **Production URL:** https://india-risk-monitor.pages.dev/
- **Repo:** `C:\Users\anike\Downloads\IRM_Build` · `git@github.com:aniketkulkarni420/india-risk-monitor.git`
- **Latest commit:** `6ac3cf9 freshness: cron redundancy + VLCC parser re-enabled`
- **Cloudflare Pages auto-deploys** on push to main (~60-90 sec)
- **Parsers:** 52 green / 0 amber / 3 red of 55 tracked (3 reds: fastag_toll, rail_freight, block_deals_notional)
- **India self-hosted runner online:** `LAPTOP-OKF7CAFA-irm` · label `india` · auto-starts via Task Scheduler
- **GitHub Secrets set:** GROQ_API_KEY, GEMINI_API_KEY, DATAGOVIN_API_KEY
- **Free LLM available:** Groq llama-3.3-70b-versatile · Gemini gemini-flash-latest
- **Public API live:** `/api/v1/metrics.json`, `/api/v1/metrics/{id}.json`, `/api/v1/composites.json`, `/api/v1/sectors.json`, `/api/v1/health.json`

---

## Scheduled reminders (auto-fire)

| Time | What | File |
|---|---|---|
| 2026-05-12 15:00 IST | First setup pass (CF token, PIB Gmail, Termux, R2, Telegram) | `~/.claude/scheduled-tasks/irm-setup-reminder-3pm/SKILL.md` |
| 2026-05-12 19:00 IST | Final setup reminder if anything pending | `~/.claude/scheduled-tasks/irm-setup-reminder-7pm/SKILL.md` |
| 2026-05-13 10:00 IST | **Deferred items review with impact/risk scorecard** | `~/.claude/scheduled-tasks/irm-deferred-items-reminder/SKILL.md` |

---

## Aniket's setup actions awaiting (one-time)

These were built today but inactive until tokens are provided:

1. **Cloudflare Worker proxy** — needs `CLOUDFLARE_API_TOKEN` (from dash.cloudflare.com/profile/api-tokens · "Edit Cloudflare Workers" template) + `WORKER_TOKEN` (any random string for runner-to-worker auth)
2. **PIB email pipeline** — needs Gmail OAuth (CLIENT_ID + CLIENT_SECRET + REFRESH_TOKEN) + PIB subscription at pib.gov.in/EmailSubscription + Gmail filter "IRM-Source" label
3. **Termux backup runner** — needs Android phone · F-Droid Termux install · run `bash <(curl -fsSL https://raw.githubusercontent.com/aniketkulkarni420/india-risk-monitor/main/scripts/install-termux-runner.sh)`
4. **R2 disaster backup** — needs R2 bucket "irm-backup" + API token (R2:Edit scope) → secrets R2_ACCOUNT_ID + R2_ACCESS_KEY_ID + R2_SECRET_ACCESS_KEY + R2_BUCKET
5. **Telegram alerts** — needs bot token from @BotFather + chat ID from @userinfobot → secrets TELEGRAM_BOT_TOKEN + TELEGRAM_CHAT_ID

When Aniket pastes any token in the next session, `gh secret set` it immediately.

---

## How to resume in next session

```bash
cd C:\Users\anike\Downloads\IRM_Build
git pull origin main
git log --oneline -10
npm run validate
node -e "const h=JSON.parse(require('fs').readFileSync('data/parser-health.json','utf8')); console.log('Health:',h.summary);"
```

**Then:**
1. Read this file end-to-end (you're doing it)
2. Open `C:\Users\anike\Downloads\IRM_Section_Mockups_2026-05-12.html` in browser
3. Open the live site https://india-risk-monitor.pages.dev/ in another tab — visually verify 2E heatmap cells + 10B pill direction are showing on MoM/YoY columns and status pills
4. Confirm with Aniket which UX picks to implement first (from "Approved UI/UX picks · NOT yet built" section above)
5. Build in priority: 1A+1B hero merge → 4D sector grid → 9D inline expand → 6A+6C watchlist → 5C timeline strip → 15 nuance chips → 11A mobile cards → 3A range tick render
6. After EACH UI change · run `npm run validate`, `npm run bundle`, verify visual change on live site, commit

**User preferences (locked):**
- Compact-first default (Aniket explicit)
- DoD always Tier 1 visible · cannot be hidden
- Single-sentence dynamic narrative max
- V60 affinity · bar trios, dense scan grids, regime notices
- Auto bar trio in Real Economy is sacred · sorted-by-MoM
- Approved decisions are LOCKED · double-check before changing
- Show before building · visual mockups before code
- Multiple options preferred over single recs
- Honest critique > sycophancy
- Punchy data-first copy · no hedging · no AI openers · no em dashes
- SEBI RA registration line in footer · "Built by Aniket Kulkarni"

---

## Things to watch for · do NOT repeat

1. **Do not re-litigate approved structures.** When in doubt ask "this would change approved X, OK?"
2. **Do not push to main without explicit "approve push"** (rule still applies in new sessions until Aniket repeats it)
3. **Do not break the live dashboard with blind UI surgery.** Test bundle + visit live site after every change.
4. **Auto bar trio in Real Economy is sacred** · sorted-by-MoM version is "the version Aniket loved"
5. **Trend column heatmap (2E) is ONLY on MoM/YoY · not on DoD/1W trends** (intra-day flap = noise)
6. **Status pill direction (10B) is derived from sparkline_12m last 3-8 points** · NOT user-configurable
7. **Range tick (3A) uses qualitative labels not p-percentile** ("Near 12m high" not "p82")
8. **GitHub free-tier cron is best-effort** · always add retry triggers for critical schedules
9. **India runner = single point of failure** until Termux backup is set up · monitor `gh api repos/.../actions/runners`
10. **Manual override layer is the human-in-the-loop guarantee** · always available · 60-second hotfix

---

## End of handoff. Read top to bottom before writing any code or proposing any design change.
